#!/bin/bash

# ============================================================================
# module_05.sh - Konfigürasyon ve Güvenlik (TAMAMLANMIŞ)
# ============================================================================

show_module_05() {
    cat << 'CONTENT_EOF'
╔══════════════════════════════════════════════════════════════════════════════╗
║              MODÜL 5: GELİŞMİŞ ÖZELLİKLER VE PRATİK ÖRNEKLER              ║
╚══════════════════════════════════════════════════════════════════════════════╝

🟦 GELİŞMİŞ PAKET YÖNETİMİ

1️⃣ PAKET SÜRÜM YÖNETİMİ VE DOWNGRADE

💻 Mevcut sürümleri listeleme:
────────────────────────────────────────────────────────────────────────────────
$ apt-cache madison firefox
────────────────────────────────────────────────────────────────────────────────

🔄 Örnek Çıktı:
────────────────────────────────────────────────────────────────────────────────
firefox | 110.0+build3-0ubuntu0.22.04.1 | jammy-updates/main amd64
firefox | 109.0+build2-0ubuntu0.22.04.1 | jammy-security/main amd64
firefox | 1:1snap1-0ubuntu2 | jammy/main amd64
────────────────────────────────────────────────────────────────────────────────

💻 Belirli sürüm yükleme:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt install firefox=109.0+build2-0ubuntu0.22.04.1
────────────────────────────────────────────────────────────────────────────────

⚠️ DİKKAT: Sürüm düşürme risklidir! Sadece gerektiğinde yapın.

2️⃣ PAKET TUTMA (HOLD) - DETAYLI

💻 Paketi güncelleme dışında bırakma:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt-mark hold firefox
firefox set on hold.
────────────────────────────────────────────────────────────────────────────────

💻 Tutulan paketleri görme:
────────────────────────────────────────────────────────────────────────────────
$ apt-mark showhold
firefox
────────────────────────────────────────────────────────────────────────────────

💻 Hold durumunu kaldırma:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt-mark unhold firefox
Canceled hold on firefox.
────────────────────────────────────────────────────────────────────────────────

🎯 KULLANIM SENARYOLARI:
• Stabil bir sürümü korumak
• Test sırasında beklenmeyen güncellemeleri önlemek
• Uyumluluk sorunlarını çözmek

3️⃣ PAKET YENIDEN KURMA VE ONARMA

💻 Paketi yeniden kur (konfigürasyon korunur):
────────────────────────────────────────────────────────────────────────────────
$ sudo apt install --reinstall firefox
────────────────────────────────────────────────────────────────────────────────

💻 Bozuk paket onarma:
────────────────────────────────────────────────────────────────────────────────
$ sudo dpkg --configure -a
$ sudo apt --fix-broken install
────────────────────────────────────────────────────────────────────────────────

💻 Eksik dosyaları geri getirme:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt install --reinstall $(dpkg -S /path/to/missing/file | cut -d: -f1)
────────────────────────────────────────────────────────────────────────────────

🟦 APT İLE TOPLU İŞLEMLER

1️⃣ TOPLU PAKET KURULUMU

💻 Liste dosyasından kurulum:
────────────────────────────────────────────────────────────────────────────────
# paketler.txt dosyası oluştur
$ cat > paketler.txt << EOF
curl
wget
git
vim
htop
neofetch
EOF

# Toplu kur
$ sudo apt install $(cat paketler.txt)
────────────────────────────────────────────────────────────────────────────────

💻 Regex ile toplu kurulum:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt install python3-*
# Tüm python3 paketleri (DİKKAT: Çok fazla paket!)
────────────────────────────────────────────────────────────────────────────────

2️⃣ TOPLU PAKET KALDIRMA

💻 Belirli pattern'e uyan paketleri kaldır:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt remove --purge $(dpkg -l | grep 'rc' | awk '{print $2}')
# rc durumundaki tüm paketleri temizle
────────────────────────────────────────────────────────────────────────────────

🟦 APT İLE SİSTEM YÖNETİMİ

1️⃣ SİSTEM YÜKSELTME (DISTRIBUTION UPGRADE)

💻 Minör sürüm yükseltme:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt update
$ sudo apt full-upgrade
────────────────────────────────────────────────────────────────────────────────

💻 Majör sürüm yükseltme (Ubuntu 22.04 → 24.04):
────────────────────────────────────────────────────────────────────────────────
$ sudo apt update && sudo apt upgrade
$ sudo apt install update-manager-core
$ sudo do-release-upgrade
────────────────────────────────────────────────────────────────────────────────

⚠️ ÖNEMLE DİKKAT:
• Önemli verileri yedekleyin
• Stabil internet bağlantısı olsun
• Elektrik kesintisine karşı önlem alın
• Test ortamında önce deneyin

2️⃣ BOZUK SİSTEM KURTARMA

💻 Acil durum onarım komutları:
────────────────────────────────────────────────────────────────────────────────
# 1. Kilit dosyalarını temizle
$ sudo rm /var/lib/dpkg/lock*
$ sudo rm /var/cache/apt/archives/lock

# 2. Dpkg veritabanını onar
$ sudo dpkg --configure -a

# 3. Kırık bağımlılıkları düzelt
$ sudo apt --fix-broken install

# 4. Tüm paketleri güncelle
$ sudo apt update && sudo apt upgrade

# 5. Otomatik temizlik
$ sudo apt autoremove
$ sudo apt autoclean
────────────────────────────────────────────────────────────────────────────────

🟦 GELİŞTİRİCİ ORTAMLARI

1️⃣ WEB GELİŞTİRİCİ ORTAMI

💻 LAMP Stack kurulumu:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt update
$ sudo apt install apache2 mysql-server php libapache2-mod-php php-mysql
$ sudo systemctl enable apache2
$ sudo systemctl enable mysql
────────────────────────────────────────────────────────────────────────────────

💻 Node.js geliştirme ortamı:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt install curl
$ curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
$ sudo apt install nodejs
$ node --version
$ npm --version
────────────────────────────────────────────────────────────────────────────────

2️⃣ PYTHON GELİŞTİRİCİ ORTAMI

💻 Tam Python stack:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt install python3 python3-pip python3-venv python3-dev
$ sudo apt install build-essential libssl-dev libffi-dev
$ python3 --version
$ pip3 --version
────────────────────────────────────────────────────────────────────────────────

💻 Sanal ortam oluşturma:
────────────────────────────────────────────────────────────────────────────────
$ python3 -m venv ~/projeler/myenv
$ source ~/projeler/myenv/bin/activate
$ pip install django flask numpy pandas
────────────────────────────────────────────────────────────────────────────────

3️⃣ SİSTEM YÖNETİCİSİ ARAÇLARI

💻 Temel sistem araçları:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt install \
    vim tmux screen \
    htop iotop nethogs \
    net-tools dnsutils \
    curl wget rsync \
    git build-essential
────────────────────────────────────────────────────────────────────────────────

🟦 APT İLE OTOMASYON

1️⃣ SCRİPT İLE PAKET YÖNETİMİ

💻 Otomatik güncelleme scripti:
────────────────────────────────────────────────────────────────────────────────
#!/bin/bash
# guncelle.sh

LOG="/var/log/apt-guncelleme.log"

echo "=== Güncelleme Başladı: $(date) ===" >> "$LOG"

# Paket listelerini güncelle
sudo apt update >> "$LOG" 2>&1

# Güncellenebilir paketleri listele
echo "Güncellenebilir paketler:" >> "$LOG"
apt list --upgradable >> "$LOG" 2>&1

# Otomatik güncelle
sudo apt upgrade -y >> "$LOG" 2>&1

# Temizlik
sudo apt autoremove -y >> "$LOG" 2>&1
sudo apt autoclean >> "$LOG" 2>&1

echo "=== Güncelleme Bitti: $(date) ===" >> "$LOG"
────────────────────────────────────────────────────────────────────────────────

💻 Crontab ile otomatik çalıştırma:
────────────────────────────────────────────────────────────────────────────────
$ sudo crontab -e

# Her Pazar 03:00'te güncelle
0 3 * * 0 /usr/local/bin/guncelle.sh
────────────────────────────────────────────────────────────────────────────────

2️⃣ PAKET KURULUM KONTROLÜ

💻 Paket yüklü mü kontrol script:
────────────────────────────────────────────────────────────────────────────────
#!/bin/bash
# paket-kontrol.sh

PAKETLER=("curl" "git" "vim" "htop")

for paket in "${PAKETLER[@]}"; do
    if dpkg -l | grep -q "^ii  $paket "; then
        echo "✅ $paket yüklü"
    else
        echo "❌ $paket yüklü değil"
        read -p "$paket kurmak ister misiniz? (e/h): " cevap
        if [[ "$cevap" == "e" ]]; then
            sudo apt install -y "$paket"
        fi
    fi
done
────────────────────────────────────────────────────────────────────────────────

🟦 PERFORMANS OPTİMİZASYONU

1️⃣ APT HIZLANDIRMA

💻 Paralel indirme aktif et:
────────────────────────────────────────────────────────────────────────────────
$ echo 'Acquire::Queue-Mode "host";' | sudo tee /etc/apt/apt.conf.d/99parallel
$ echo 'Acquire::http::Pipeline-Depth "5";' | sudo tee -a /etc/apt/apt.conf.d/99parallel
────────────────────────────────────────────────────────────────────────────────

💻 En hızlı mirror'u bul:
────────────────────────────────────────────────────────────────────────────────
$ sudo apt install apt-fast
# apt-fast, apt'nin hızlı alternatifidir
────────────────────────────────────────────────────────────────────────────────

2️⃣ ÖNBELLEK OPTİMİZASYONU

💻 Önbellek boyutu sınırlama:
────────────────────────────────────────────────────────────────────────────────
$ echo 'APT::Cache-Limit "50000000";' | sudo tee /etc/apt/apt.conf.d/00cache
────────────────────────────────────────────────────────────────────────────────

🟦 PRATİK SENARYOLAR

1️⃣ SENARYO: Yeni Bir Sunucu Kurulumu

💻 İlk adımlar:
────────────────────────────────────────────────────────────────────────────────
# 1. Sistemi güncelle
sudo apt update && sudo apt upgrade -y

# 2. Temel araçları kur
sudo apt install -y curl wget git vim ufw fail2ban

# 3. Güvenlik duvarını yapılandır
sudo ufw allow 22
sudo ufw enable

# 4. Otomatik güncellemeleri etkinleştir
sudo apt install -y unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades

# 5. Gereksiz paketleri temizle
sudo apt autoremove -y
────────────────────────────────────────────────────────────────────────────────

2️⃣ SENARYO: Disk Alanı Temizliği

💻 Komut dizisi:
────────────────────────────────────────────────────────────────────────────────
# Mevcut durumu kontrol et
df -h /

# Büyük paketleri listele
dpkg-query -W -f='${Installed-Size}\t${Package}\n' | sort -rn | head -20

# Önbelleği temizle
sudo apt clean
sudo apt autoclean

# Kullanılmayan paketleri kaldır
sudo apt autoremove --purge

# Journal loglarını temizle (systemd)
sudo journalctl --vacuum-time=7d

# Yeni durumu kontrol et
df -h /
────────────────────────────────────────────────────────────────────────────────

3️⃣ SENARYO: Sistem Klonlama

💻 Bir sistemin paket listesini başka sisteme taşıma:
────────────────────────────────────────────────────────────────────────────────
# Kaynak sistemde:
dpkg --get-selections > paketler.txt
cat /etc/apt/sources.list > sources.txt
ls /etc/apt/sources.list.d/ > sources-list.txt

# Hedef sistemde:
sudo apt update
sudo dpkg --set-selections < paketler.txt
sudo apt-get dselect-upgrade -y
────────────────────────────────────────────────────────────────────────────────

🟦 SORUN GİDERME REHBERİ

┌──────────────────────────┬─────────────────────────────────────────────────┐
│         SORUN            │                  ÇÖZÜM                          │
├──────────────────────────┼─────────────────────────────────────────────────┤
│ Hash Sum mismatch        │ sudo rm -rf /var/lib/apt/lists/*                │
│                          │ sudo apt update                                 │
├──────────────────────────┼─────────────────────────────────────────────────┤
│ Unable to fetch          │ sources.list'i kontrol et                       │
│                          │ İnternet bağlantısını kontrol et               │
├──────────────────────────┼─────────────────────────────────────────────────┤
│ Unmet dependencies       │ sudo apt --fix-broken install                   │
├──────────────────────────┼─────────────────────────────────────────────────┤
│ Package not found        │ sudo apt update                                 │
│                          │ sources.list'e gerekli depoyu ekle             │
├──────────────────────────┼─────────────────────────────────────────────────┤
│ dpkg interrupted         │ sudo dpkg --configure -a                        │
└──────────────────────────┴─────────────────────────────────────────────────┘

🎉 TEBRİKLER! APT REHBERİNİ TAMAMLADINIZ!

Artık APT paket yöneticisini profesyonel seviyede kullanabilirsiniz. 
Bu bilgilerle:

✅ Sistem yönetimi yapabilirsiniz
✅ Geliştirici ortamları kurabilirsiniz
✅ Güvenlik yapılandırmaları yapabilirsiniz
✅ Sorunları bağımsız çözebilirsiniz
✅ Otomatize sistemler kurabilirsiniz

🚀 SAĞ PANELDEKİ TERMİNALDE DENEYEBİLECEĞİNİZ KOMUTLAR:

🔒 GÜVENLİ KOMUTLAR:
• apt-cache stats
• dpkg -l | grep ^ii | wc -l
• apt-cache madison firefox
• apt-mark showhold
• df -h /var/cache/apt/archives

⚠️ TEST ORTAMINDA DENEYİN:
• sudo apt install neofetch
• neofetch
• sudo apt-mark hold neofetch
• sudo apt-mark showhold
• sudo apt-mark unhold neofetch

💡 İPUCU: Öğrendiklerinizi test ortamında pratik yaparak pekiştirin!

🔗 SONRAKİ ADIMLAR:
• Docker ve container teknolojilerini öğrenin
• Snap ve Flatpak paket sistemlerini inceleyin
• Diğer dağıtımların paket yöneticilerini keşfedin (dnf, pacman, zypper)
• Sistem otomasyon araçlarını öğrenin (Ansible, Puppet)

📚 KAYNAKLAR:
• man apt
• man apt-get
• man dpkg
• https://wiki.debian.org/Apt
• https://help.ubuntu.com/community/AptGet/Howto

CONTENT_EOF
}
